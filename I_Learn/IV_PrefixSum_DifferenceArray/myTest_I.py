"""
差分与前缀和自我测试 Pass ✅

游乐园人流监控系统题目描述：游乐园有 N 个主题区域（编号 0 到 N-1）。
早晨开园时，每个区域已经有了一批初始游客，记录在 initial_crowd 中。在接下来的一小时内，
发生了 M 次旅行团的集体移动，每次记录为 [start, end, count]，表示从 start 到 end 区域
每个区域都涌入了 count 名游客（如果为负，表示离开）。
一小时后，移动结束，园区主管开始不断向你提问：“第 L 个到第 R 个区域，现在总共有多少人？”
请你设计一个系统，极速处理所有的移动，并能瞬间回答主管的每一次查询。
"""

class CrowdControl():
    def __init__(self, initial_crowd: list[int]):
        self.n = len(initial_crowd)
        self.diff = [0] * (self.n + 1)
        self.diff[0] = initial_crowd[0]          # 0 站必须手动初始化，否则谁也不知道他是多少
        for i in range(1,self.n):                # 同样从1开始
            self.diff[i] = initial_crowd[i] - initial_crowd[i-1]
                                                 # 这里也同样不会越界
    # 从零开始计算差分数组 
    def move(self,next:list[list[int]]) -> list[int]:
        for left , right , crowd_nums in next:   # 🌟 这里反而是使用嵌套list更加方便
            self.diff[left] += crowd_nums
            self.diff[right + 1] -= crowd_nums   # 🌟 尾部哨兵：这里其实有没有不影响结果，因为在最后一个
        return self.diff

    # 接收差分数组，计算结果
    def getRealTimePeople(self,action:list[int]) -> list[int]:
        prefix = [0] * self.n
        prefix[0] = action[0]                    # 我只是想把真实数值反推出来，所以长度是 n 
        for i in range(1,self.n):                # 🌟 从差分还原真实数组首位不用哨兵，且for从1开始
            prefix[i] = prefix[i-1] + action[i]  # 由于从1开始也不需要防越界
        return prefix


if __name__ == "__main__":
    initial = [100, 100, 100, 100, 100]
    cro = CrowdControl(initial)  # 初始化初始区域人数

    print("开始移动...")
    move_list = cro.move([[0, 2, 50],[2, 4, -20]])  
     # 0,1,2 区域各进 50 人 | 2,3,4 区域各走 20 人 (尾部更新砸在哨兵上)

    res = cro.getRealTimePeople(move_list)

    print(res)



"""
Final Details

🌟 尾部哨兵并不一定是在整体list的结尾，指的是每次差分后的[i+1]位，如果确实是list的最后一个那确实是有没有不影响结果

这个前缀和，有时候从1开始，有时候0为哨兵，这里又设置为action[0]，为什么，到底怎么用
这个问题问得太绝了！你正好戳中了全网无数算法新手（甚至部分老手）最容易搞混的一个死角。
之所以你会看到各种“加一”、“从1开始”、“设0为哨兵”的版本，是因为大家在教算法的时候，把两种完全不同用途的“前缀和”混为一谈了！**
前缀和其实有两个截然不同的应用场景。我们直接把它们掰开揉碎对比一下，你以后一秒钟就能分清该用哪种。

----------------------------------------
场景一：“滚雪球” —— 为了还原真实数组（就是你写的这段代码）

你的目的：你手里拿着一个“变化量数组”（差分数组），你想把这些变化量像滚雪球一样累加起来，还原出现在的真实人数。
你的诉求：我只想要一个和原来长度一模一样（长度为 N）的真实数组。

初始化：prefix = [0] * self.n （因为真实区域就是 n 个，不需要多不少）
第 0 项怎么设：prefix[0] = action[0] （第 0 个区域的真实人数，就是差分数组的第 0 项本身，所以直接赋值）
循环怎么写：for i in range(1, self.n) （既然第 0 项已经有值了，那当然是从第 1 项开始，用前一项加上当前的变化量）
它防御了什么：由于i从1开始，完全不需要防御任何越界，算完后这个数组直接作为结果返回（例如 [150, 150, 130, 80, 80]）

> 💡 这种场景下，尽量别给变量起名叫 prefix，叫 real_nums、restore_arr 或者 res 更好。
      虽然用了前缀和计算方式，但它身份是“最终真实结果”。

----------------------------------------
场景二：“查字典” —— 为了应对频繁的区间求和查询

你的目的：你手里拿着一个真实的数组，老板要求你随时回答：“第 L 到第 R 项的总和是多少？”
你的诉求：我要造一本“字典”，通过 字典[R+1] - 字典[L]（大面积减去小面积）的绝杀公式，瞬间算出答案。

初始化：prefix = [0] * (self.n + 1) （必须加 1，留出头部哨兵的位置）
第 0 项怎么设：prefix[0] = 0 （这就是头部哨兵，物理意义是“前 0 个元素的和是 0”）
循环怎么写：
    ```
    for i in range(self.n):  # 从 0 遍历到 n-1
        prefix[i+1] = prefix[i] + nums[i]
    ```

（注意这里的错位：prefix[1] 存的是 nums[0] 的和，prefix[2] 存的是前两个的和……）
它防御了什么：它防御了老板查询 [0, 2] 时，公式变成 prefix[3] - prefix[0]。如果头部没有 0 哨兵，减去第 0 项就会变成无源之水。

----------------------------------------
终极一图胜千言

| 对比维度 | 场景一：“滚雪球”还原 (你上面的代码) | 场景二：“查字典”查询 (经典前缀和) |
| --- | --- | --- |
| **主要目的** | 从差分数组还原出真实数组 | 面对真实数组，准备应付区间查询 |
| **数组长度** | N (和原数组一样) | N + 1 (比原数组多 1 个头部哨兵) |
| **首个元素** | res[0] = nums[0] (直接继承原值) | prefix[0] = 0 (人造头部哨兵) |
| **遍历起点** | range(1, N) | range(0, N) (填充`prefix[i+1]) |
| **后继操作** | 算完就结束，作为结果返回 | 算完不结束，留着做 P[R+1] - P[L] 的减法 |

以后遇到前缀和，先问自己一句：“我是要还原数组，还是要造字典查区间和？” 问题瞬间迎刃而解！

"""


